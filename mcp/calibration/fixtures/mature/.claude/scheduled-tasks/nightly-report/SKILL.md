---
name: nightly-report
description: Runs at 03:00 and produces a nightly summary that feeds the morning standup.
---

# Nightly report

Scheduled task. Downstream consumer: morning standup (which reads from pvs_runs).
