# Workflow

## Intent
- Prefer explicit, well-named functions over clever one-liners — a future reader should understand intent without a debugger.
- Always run tests before committing, and fix anything that breaks instead of skipping.
